<div id="<%= dom_id device %>">
    <p>
        <strong>Name:</strong>
        <%= device.name %>
    </p>

    <p>
        <strong>Category:</strong>
        <%= device.category.name %>
    </p>

    <p>
        <stong>Manufacturer:</strong>
        <%= device.manufacturer.name %>
    </p>
    
    <p>
        <strong>Assigned To:</strong>
        <% if device.employee %>
            <%= device.employee.full_name %>
        <% end %>
    </p>
</div>
